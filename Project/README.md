# Проект: AI-агент очищення даних

**Виконав:** Голдовський Олександр Васильович  
**Варіант:** 3  
**Дата виконання:** 09.05.2026  
**Дисципліна:** Технології збору та обробки даних

---

## Тема

Агент очищення даних — автоматичне виявлення пропусків, аномалій і шуму в датасетах енергоспоживання. Методи: IQR, Isolation Forest, лінійна інтерполяція, RobustScaler. Інтерфейс: CLI. Агент самостійно аналізує дані і приймає рішення про методи обробки.

---

## Датасет

**Назва:** PJM Hourly Energy Consumption — AEP Region  
**Джерело:** [Kaggle — robikscube/hourly-energy-consumption](https://www.kaggle.com/datasets/robikscube/hourly-energy-consumption)  
**Файл:** `AEP_hourly.csv`  
**Обсяг:** 121 273 рядки погодинних вимірювань (за замовчуванням завантажується 10 000)  
**Колонки:** `Datetime` (дата/час), `MW` (енергоспоживання у мегаватах)

Для демонстрації агента до чистих даних штучно вводяться пропуски (4%) та аномалії (3%), після чого агент їх виявляє і виправляє. Метрики розраховуються порівняно з відомими "правдивими" мітками.

---

## Архітектура агента

Агент побудований за принципом циклу **ReAct (Reason + Act)**:

```
Сприйняття → Діагностика → Міркування → Рішення → Дія → Оцінка
```

| Компонент | Метод класу | Опис |
|---|---|---|
| CLI-інтерфейс | `cli_intro`, `cli_ask_*` | Діалог з користувачем у терміналі |
| Сприйняття | `load_data()` | Завантаження та первинний аналіз |
| Симуляція проблем | `inject_problems()` | Введення штучних пропусків та аномалій |
| Діагностика | `diagnose()` | Аналіз % пропусків, асиметрії, IQR-аномалій |
| Рішення 1 | `diagnose()` | Вибір методу заповнення пропусків |
| Рішення 2 | `diagnose()` | Визначення потреби в масштабуванні |
| Дія 1 | `fill_missing()` | Заповнення пропусків обраним методом |
| Дія 2 | `detect_anomalies_iqr()` | Виявлення аномалій методом IQR |
| Дія 3 | `detect_anomalies_if()` | Виявлення аномалій Isolation Forest |
| Дія 4 | `combine_and_fix()` | Об'єднання результатів та виправлення |
| Дія 5 | `apply_robust_scaling()` | RobustScaler (якщо агент вирішив) |
| Оцінка | `compute_metrics()` | Precision, Recall, F1, RMSE, MAE |
| Візуалізація | `visualize()` | 4 графіки: оригінал → бруд → виявлення → очищення |
| Звіт | `print_final_report()` | Повний звіт з рішеннями агента |

---

## Логіка прийняття рішень агента

### Рішення 1 — метод заповнення пропусків

| Умова | Рішення агента |
|---|---|
| Пропусків < 2% | Лінійна інтерполяція |
| Пропусків 2–8% | Заповнення медіаною |
| Пропусків > 8% | Заповнення середнім |

### Рішення 2 — масштабування

| Умова | Рішення агента |
|---|---|
| Асиметрія (skew) > 1.0 | Застосувати RobustScaler |
| Асиметрія ≤ 1.0 | Масштабування не потрібне |

### Рішення 3 — консолідація аномалій

Агент об'єднує результати IQR і Isolation Forest через логічне АБО — точка вважається аномалією якщо хоча б один метод її виявив.

---

## Методи виявлення аномалій

### IQR (Interquartile Range)

Статистичний метод. Точки за межами `[Q1 - 1.5·IQR, Q3 + 1.5·IQR]` вважаються аномаліями. Швидкий, але чутливий лише до крайніх значень.

### Isolation Forest

Алгоритм машинного навчання на основі випадкових дерев. Аномальні точки ізолюються швидше ніж нормальні, оскільки вони рідкісні та відрізняються від більшості. Параметр `contamination` встановлюється користувачем через CLI.

---

## Метрики якості

| Метрика | Опис |
|---|---|
| Precision | Частка правильно виявлених аномалій серед всіх виявлених |
| Recall | Частка знайдених аномалій серед всіх реальних |
| F1-score | Гармонічне середнє Precision і Recall |
| RMSE | Середньоквадратична похибка між очищеними і оригінальними значеннями |
| MAE | Середня абсолютна похибка |

---

## Використані бібліотеки

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from sklearn.ensemble import IsolationForest
from sklearn.metrics import f1_score, precision_score, recall_score
from sklearn.preprocessing import RobustScaler
```

Встановлення:
```
pip install pandas numpy matplotlib scikit-learn
```

---

## Інтерфейс CLI — приклади сесій

### Тестовий запит 1 — стандартні налаштування

```
Введіть ім'я CSV файлу [AEP_hourly.csv]: AEP_hourly.csv
Кількість рядків для аналізу [10000]: 10000
Рівень чутливості до аномалій [2]: 2
Зберегти очищений датасет у CSV? [y/n, default=y]: y
```

<!-- ВСТАВИТИ: скріншот повного виводу терміналу для цього запиту -->

### Тестовий запит 2 — менше даних, висока чутливість

```
Введіть ім'я CSV файлу [AEP_hourly.csv]: AEP_hourly.csv
Кількість рядків для аналізу [10000]: 3000
Рівень чутливості до аномалій [2]: 3
Зберегти очищений датасет у CSV? [y/n, default=y]: n
```

<!-- ВСТАВИТИ: скріншот виводу (особливо блок МЕТРИКИ та РІШЕННЯ) -->

### Тестовий запит 3 — низька чутливість

```
Введіть ім'я CSV файлу [AEP_hourly.csv]: AEP_hourly.csv
Кількість рядків для аналізу [10000]: 5000
Рівень чутливості до аномалій [2]: 1
Зберегти очищений датасет у CSV? [y/n, default=y]: y
```

<!-- ВСТАВИТИ: скріншот виводу з іншими значеннями метрик порівняно з запитом 1 -->

---

## Опис кроків роботи

### Крок 1 — Завантаження даних

<!-- ВСТАВИТИ: скріншот виводу блоку "КРОК 1: СПРИЙНЯТТЯ ДАНИХ" -->

### Крок 2 — Симуляція проблем

<!-- ВСТАВИТИ: скріншот виводу блоку "КРОК 2: СИМУЛЯЦІЯ ПРОБЛЕМ" -->

### Крок 3 — Діагностика та рішення агента

<!-- ВСТАВИТИ: скріншот виводу блоку "КРОК 3: ДІАГНОСТИКА" разом з блоком "АГЕНТ ПРИЙМАЄ РІШЕННЯ" -->

### Кроки 4–7 — Pipeline очищення

<!-- ВСТАВИТИ: скріншот виводу кроків 4, 5, 6, 7 одним великим скріншотом -->

### Крок 8 — Масштабування (якщо застосоване)

<!-- ВСТАВИТИ: скріншот виводу "КРОК 8: ROBUST МАСШТАБУВАННЯ" (або пропустити якщо агент вирішив не застосовувати) -->

### Крок 9 — Метрики якості

<!-- ВСТАВИТИ: скріншот виводу "КРОК 9: МЕТРИКИ ЯКОСТІ АГЕНТА" -->

### Крок 10 — Візуалізація

<!-- ВСТАВИТИ: зображення файлу cleaning_result.png -->

### Підсумковий звіт

<!-- ВСТАВИТИ: скріншот виводу "ПІДСУМКОВИЙ ЗВІТ АГЕНТА" з рішеннями та метриками -->

---

## Аналіз переваг і недоліків агента

**Переваги:**
- Автоматичне прийняття рішень без участі користувача
- Комбінування двох методів виявлення аномалій підвищує Recall
- Логування кожного кроку дозволяє відстежити логіку агента
- Кількісні метрики (F1, RMSE) дозволяють об'єктивно оцінити якість

**Недоліки:**
- Симуляція проблем не замінює реальних забруднених даних
- Isolation Forest чутливий до параметра contamination
- Лінійна інтерполяція не завжди відновлює реальну форму сигналу

---

## Висновок

Розроблений AI-агент реалізує повний автоматизований pipeline очищення часових рядів енергоспоживання. Агент отримує параметри через CLI, самостійно аналізує статистичні характеристики даних і приймає рішення про методи обробки залежно від виявлених проблем. Комбінація IQR і Isolation Forest забезпечує більш повне виявлення аномалій ніж кожен метод окремо. Метрики Precision, Recall, F1, RMSE і MAE дозволяють кількісно оцінити якість роботи агента. Архітектура відповідає концепції ReAct-агента: цикл сприйняття, міркування, дії та оцінки.

---

## Як запустити

1. Завантажити `AEP_hourly.csv` з [Kaggle](https://www.kaggle.com/datasets/robikscube/hourly-energy-consumption)
2. Покласти файл у папку `Project/` поруч з `main.py`
3. Встановити залежності: `pip install pandas numpy matplotlib scikit-learn`
4. Запустити: `python main.py`
5. Відповісти на запитання агента у терміналі
